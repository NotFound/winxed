function doit(string h)
{
	if (h != "Hola")
		say(h, " es hola");
	else
		say(h, " no lo es");
}

function main()
{
	int i;
	i = 1 == 2;
	say(i);
	string h = "Hola";
	int j;
	if (h == "Hola")
		say("Yeah");
	doit(h);
	doit("Hola");
	doit("No");
	string s = "tampoco";
	doit(s);

	i = 1 || 0;
	say(i);

	i = 1 == 2 && (2  + 1) == 3;
	say(i);
}
