function main()
{
	var i;
	var s = new 'String';
	s= "Hi";
	for (i in [1, 2, 3, 4, s, 5, 6, 7, 8, 9, "Last" ] )
		say(i);
	say ('End.');

	var a;
	var it;
	for (it in [ [1, 2 ], [ 3, 4 ] ] )
	{
		var it2;
		for (it2 in it)
			say (it2);
		say();
	}
	for (it in { a: 1 } )
		say();
}
