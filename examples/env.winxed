# !winxed

// Example of accessing environment variables
// using parrot PMC 'Env'.

function main()
{
    var env = new 'Env';
    string var;
    for (var in env)
        say(var, '=', env[var]);
}
