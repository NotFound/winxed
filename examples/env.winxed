# !winxed

// Example of accessing environment variables
// using parrot PMC 'Env'.

function main()
{
    var env = new 'Env';
    for (string varname in env)
        say(varname, '=', env[varname]);
}
